syntax = "proto3";

package test;

option go_package = "gitlab.com/gosharmot/test/pkg/test;test";

import "google/protobuf/wrappers.proto";

service Test {
  rpc Call(msg) returns (msg) {}
}

message msg {
  string msg = 1;
}